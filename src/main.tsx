import React from 'react'
import ReactDOM from 'react-dom/client'
import AppCompact from './AppCompact.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppCompact />
  </React.StrictMode>,
)